# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# Ignore bundler config.
/.bundle

# Ignore all environment files (except templates).
/.env*
!/.env*.erb

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

/public/assets

# Ignore master key for decrypting credentials and more.
/config/master.key
# Logs
/log/*
!log/.keep

# Temporales y PID
/tmp
/tmp/*
!/tmp/.keep
!/tmp/pids/.keep

# Archivos de configuración local
/config/database.yml
/config/master.key

# Archivos de credenciales (en versiones recientes de Rails)
/config/credentials.yml.enc

# Archivos de esquema de base de datos
/db/*.sqlite3
/db/*.sqlite3-journal

# Archivos generados por dependencias
/vendor/bundle
/vendor/cache
/vendor/ruby

# Carpetas de compilación de activos
/public/assets
/public/packs
/public/packs-test
/public/packs-dev

# Archivos de paquetes de JavaScript (npm/yarn)
/node_modules
/yarn-error.log
npm-debug.log

# Archivos de configuración de sistema operativo o IDEs
*.swp
*.DS_Store
.idea/
.vscode/

# Archivos temporales de tests
/test/tmp
/test/version_tmp

# Archivos de cobertura de pruebas
/coverage/

# Gemfile.lock, si estás usando varias versiones de Ruby o Gemfiles
# Gemfile.lock, para evitar conflictos entre sistemas operativos
/gemfile.lock

# Archivos generados por Spring
.spring/*
.sprockets-manifest-*

# Archivos de entorno
.env
.env.development.local
.env.test.local
.env.production.local

# Archivos de Docker
docker-compose.yml
